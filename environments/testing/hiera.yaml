---
:backends:
  - yaml
  - json
:yaml:
  :datadir: /etc/puppet/environments/testing/hieradata
:json:
  :datadir: /etc/puppet/environments/testing/hieradata
:hierarchy:
  - "%{::clientcert}"
  - "%{::osfamily}"
  - "%{hostname}"
  - "nodes/%{::fqdn}"
  - common
